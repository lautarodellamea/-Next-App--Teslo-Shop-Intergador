import { Size } from "@/interfaces"

interface Props {
  selectedSize: Size // talla seleccionada
  availableSizes: Size[] // tallas disponibles

}


export const SizeSelector = ({ selectedSize, availableSizes }) => {
  return (
    <div>

    </div>
  )
}